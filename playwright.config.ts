import { createUIConfig } from '@test-utils';

/**
 * Root Playwright Configuration
 * This provides a default configuration when running tests from the workspace root
 * Individual test suites have their own optimized configurations
 */
export default createUIConfig();
